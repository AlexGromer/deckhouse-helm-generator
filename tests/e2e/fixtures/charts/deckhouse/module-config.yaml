apiVersion: deckhouse.io/v1alpha1
kind: ModuleConfig
metadata:
  name: monitoring
  labels:
    app: monitoring
spec:
  enabled: true
  version: 1
  settings:
    grafana:
      enabled: true
    prometheus:
      retentionDays: 30
