apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: default
  labels:
    app.kubernetes.io/name: web-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: web-app
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
